# AI 语言尸体检测关键词

以下关键词用于检测文本中的 AI 痕迹过重表达（"语言尸体"）。
此列表是 `query.py` 中 `_handle_style_check()` 的权威来源。

## 通用陈词滥调

- 暮色从四面八方
- 空气仿佛凝固
- 时间仿佛停止
- 一股莫名的
- 不知为何
- 心中涌起一股
- 眼神中闪过一丝
- 嘴角露出一抹
- 深吸一口气
- 整个世界都
- 命运的车轮

## 使用方式

`query.py` 中的 `_handle_style_check()` 读取此列表进行检测。
如需更新检测关键词，修改此文件即可。
